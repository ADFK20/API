"use strict";
// import sharp from 'sharp'
// import path from 'path'
// import {response, request} from "express"
// async function imageResize( name:string, width:number, height:number) {
//      name = request.query.fileName as string;
//      width = request.query.width as unknown as number;
//      height = request.query.height as unknown as number;
//        await sharp(`
//         ${path.resolve("./")}\\images\\${name}.jpg`)
//         .resize(width, height).png()
//         .toFile(`${path.resolve()}\\images\\${name}.jpg`);
//         return (`${path.resolve()}\\created\\images\\${name}.jpg`)
//         }
// export default imageResize;
